services:
  mongo:
    image: mongo:7
    container_name: noghost-mongo
    restart: unless-stopped
    ports:
      - "27018:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: noghost
      MONGO_INITDB_ROOT_PASSWORD: noghost
      MONGO_INITDB_DATABASE: noghost
    volumes:
      - noghost-mongo-data:/data/db

volumes:
  noghost-mongo-data:
